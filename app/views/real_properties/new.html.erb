<div class="jumbotron jumbotron-fluid">
    <div class="container">
      <h1 class="display-4 text-white ">理想ではなくベストなお部屋さがしを</h1>
      <p class="lead text-white">"住まトク"は出来るだけ"お得に"物件を探したい方</br>
      どれだけ家賃が下がっているかをリアルタイムでお調べします</p>
    </div><!-- /.container -->
  </div><!-- /.jumbotron -->
  <div class="container">
    <div class="row">
      <div>
      <h2 class="cover-heading">ここにURLを入力してください</h2>
<!-- form_with部分 -->
        <%= form_with model: @url, url:real_properties_path, local:true do |f| %>
          <%= f.text_field :url %>
          <%= f.submit '実行' %>
        <% end %>
      </div>
    </div> <!-- /.row -->
    <%= form_for(@area) do |f| %>
    <div class="field"><!-- 都道府県 -->
      <%= f.label :prefecture_id, "都道府県" %><br>
      <%= f.collection_select :prefecture_id, Prefecture.all, :id, :name, :include_blank => true %>
      <!-- デフォルトでid: "user_prefecture_id" がついてる -->
    </div>

    <div class="field"><!-- 市町村 -->
      <div id="city-area">
        <%= render partial: 'areas/select_city', locals: {cities: @cities} %>
      </div>
    </div>
    <% end %>
  </div>
  